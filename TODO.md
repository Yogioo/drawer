# 项目待办

## 当前状态

- [x] 将画布从 tldraw 替换为 Excalidraw
- [x] 保留无限画布、选择、缩放、撤销、重做和本地保存
- [x] 重写 AI 请求链路和 SSE 错误反馈
- [x] 支持 AI 创建、修改、移动、删除和清空画布元素
- [x] 保留 OpenAI Chat Completions 兼容接口
- [x] 移除 tldraw 依赖、旧模板源码和生产域名配置
- [x] README 改为中文
- [x] 确保 `.dev.vars`、构建产物和本地运行目录被 Git 忽略

## Tauri 桌面应用

- [x] 在当前 Vite + React 项目中初始化 Tauri 2
- [x] 配置 Tauri 开发命令和生产构建目录
- [x] 配置 Windows 应用名称、图标、窗口尺寸和最小尺寸
- [x] 让桌面端默认加载本地构建资源
- [x] 保留浏览器端开发模式，不影响 `npm run dev`
- [x] 配置单实例窗口并在重复启动时唤回主窗口
- [ ] 生成 Windows 安装包并验证安装、启动和卸载
- [ ] 评估是否需要 macOS 和 Linux 构建目标

## AI 后端

### 第一阶段：远程 Worker

- [ ] 将 AI Worker 部署到稳定的 HTTPS 地址
- [x] 桌面端和浏览器端统一请求远程 `/stream`
- [ ] 将 API key 保留在 Worker 环境变量中
- [ ] 增加上游请求超时、重试和错误分类
- [ ] 增加生产环境 CORS 和请求来源限制
- [ ] 增加请求日志，但不记录 API key、完整提示词或画布敏感内容

### 可选：桌面端本地 AI 服务

- [ ] 评估是否需要 Rust 请求层替代远程 Worker
- [ ] 使用操作系统凭据存储保存用户配置
- [ ] 避免将 key 编译进前端资源
- [ ] 评估本地服务对启动时间、内存和 CPU 的影响

## 低资源目标

- [ ] 建立启动后的 CPU 和内存基线
- [ ] 记录空画布、复杂画布和 AI 请求期间的资源占用
- [ ] 画布状态更新采用防抖持久化
- [ ] 只向 AI 发送当前视口和必要的元素摘要
- [ ] 默认不生成和上传画布截图
- [ ] 避免重复序列化大型画布数据
- [ ] 发布版关闭开发日志、HMR 和调试入口
- [ ] 检查 Excalidraw 和 Mermaid 相关依赖的包体积
- [ ] 评估按需加载导出、图片和 Mermaid 功能
- [ ] 确认单窗口运行，不启动 Node 或 Cloudflare sidecar

## AI 画布能力

- [x] 增加 AI 批量对齐、分布和排序操作
- [x] 增加箭头与元素之间的绑定关系
- [ ] 增加 AI 读取图片元素的能力
- [x] 增加 AI 操作前的预览和撤销
- [x] 增加操作历史和失败操作恢复
- [x] 完善模型返回 JSON 的校验和修复
- [ ] 增加真实兼容服务的端到端测试

## 测试和发布验收

- [x] `npx tsc -b --pretty false` 通过
- [x] `npm run build` 通过
- [x] 无 API key 时聊天面板显示明确错误
- [x] mock Chat Completions 可以返回创建元素和消息事件
- [ ] 浏览器中确认 Excalidraw 可绘制、选择、移动和删除
- [ ] 浏览器中确认 AI 创建的元素可见且可继续编辑
- [ ] 浏览器中确认刷新后画布仍能恢复
- [ ] 浏览器中确认 AI 错误不会让界面卡死
- [x] Tauri 开发模式和发布模式分别通过基础启动 smoke test
- [ ] Windows 安装包在干净环境中验证
- [ ] 发布前运行 `npm audit` 并处理高风险依赖

## 约束

- 不再引入 tldraw SDK
- 不在 README、源码或提交记录中写入真实 API key
- 不在 README 中写入个人中转站地址
- `.dev.vars` 只用于本地配置，并保持 Git 忽略
- 优先使用远程 Worker，避免桌面端额外启动本地服务
